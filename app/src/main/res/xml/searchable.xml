<?xml version="1.0" encoding="utf-8"?>
<searchable xmlns:android="http://schemas.android.com/apk/res/android"
    android:hint="@string/search_hint"
    android:includeInGlobalSearch="true"
    android:label="@string/app_name"
    android:searchSuggestAuthority="org.bavand.ui.search.SuggestionProvider"
    android:searchSuggestSelection=" ?"
    android:voiceLanguage="@string/voicelanguage"
    android:voiceLanguageModel="@string/voicelanguagemodel"
    android:voicePromptText="@string/search_hint"
    android:voiceSearchMode="launchRecognizer|showVoiceSearchButton">

</searchable>
